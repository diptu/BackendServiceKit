# Environment variables injected into every act container run.
# Safe to commit — no credentials live here.
# These mirror the 'env:' blocks in .github/workflows/*.yml exactly.

# Matches ci.yml job-level env and conftest.py DATABASE_URL override
DATABASE_URL=sqlite+aiosqlite:///test_db.sqlite
