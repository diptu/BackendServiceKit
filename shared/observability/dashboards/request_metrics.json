{
  "title": "Request Metrics",
  "description": "No panel here has real backing data today. Every query assumes shared/observability/metrics/* (the OTel Metrics SDK constructors in counters.py/histograms.py) is wired into Tenent/APIGateway and exporting http_requests_total / http_request_duration_seconds_bucket. It isn't (verified: zero references to that module anywhere in either service's code) and no other code path emits an equivalently-named metric either. Wiring it in is out of scope for services/MetricsCollection (see its TODO.md Decision #1) — that would be an edit to Tenent/APIGateway's own code.",
  "uid": "request-metrics",
  "schemaVersion": 38,
  "version": 1,
  "refresh": "30s",
  "time": {"from": "now-1h", "to": "now"},
  "panels": [
    {
      "id": 1,
      "title": "Request Rate (req/s)",
      "type": "timeseries",
      "gridPos": {"x": 0, "y": 0, "w": 12, "h": 8},
      "targets": [
        {
          "datasource": {"type": "prometheus"},
          "expr": "sum(rate(http_requests_total[5m])) by (service)",
          "legendFormat": "{{service}}"
        }
      ]
    },
    {
      "id": 2,
      "title": "Error Rate (%)",
      "type": "timeseries",
      "gridPos": {"x": 12, "y": 0, "w": 12, "h": 8},
      "targets": [
        {
          "datasource": {"type": "prometheus"},
          "expr": "100 * sum(rate(http_requests_total{status_code=~\"5..\"}[5m])) by (service) / sum(rate(http_requests_total[5m])) by (service)",
          "legendFormat": "{{service}}"
        }
      ],
      "fieldConfig": {"defaults": {"unit": "percent"}}
    },
    {
      "id": 3,
      "title": "Total Requests (last 1h)",
      "type": "stat",
      "gridPos": {"x": 0, "y": 8, "w": 6, "h": 4},
      "targets": [
        {
          "datasource": {"type": "prometheus"},
          "expr": "sum(increase(http_requests_total[1h]))"
        }
      ]
    },
    {
      "id": 4,
      "title": "Total 5xx Errors (last 1h)",
      "type": "stat",
      "gridPos": {"x": 6, "y": 8, "w": 6, "h": 4},
      "targets": [
        {
          "datasource": {"type": "prometheus"},
          "expr": "sum(increase(http_requests_total{status_code=~\"5..\"}[1h]))"
        }
      ],
      "fieldConfig": {"defaults": {"color": {"mode": "thresholds"}, "thresholds": {"steps": [{"color": "green", "value": 0}, {"color": "red", "value": 1}]}}}
    }
  ]
}
